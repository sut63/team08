// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"github.com/facebookincubator/ent/dialect/sql"
	"github.com/sut63/team08/ent/tool"
)

// Tool is the model entity for the Tool schema.
type Tool struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// ToolName holds the value of the "Tool_Name" field.
	ToolName string `json:"Tool_Name,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ToolQuery when eager-loading is set.
	Edges ToolEdges `json:"edges"`
}

// ToolEdges holds the relations/edges for other nodes in the graph.
type ToolEdges struct {
	// ToolOperativerecord holds the value of the Tool_Operativerecord edge.
	ToolOperativerecord []*Operativerecord
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ToolOperativerecordOrErr returns the ToolOperativerecord value or an error if the edge
// was not loaded in eager-loading.
func (e ToolEdges) ToolOperativerecordOrErr() ([]*Operativerecord, error) {
	if e.loadedTypes[0] {
		return e.ToolOperativerecord, nil
	}
	return nil, &NotLoadedError{edge: "Tool_Operativerecord"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Tool) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{},  // id
		&sql.NullString{}, // Tool_Name
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Tool fields.
func (t *Tool) assignValues(values ...interface{}) error {
	if m, n := len(values), len(tool.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	t.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field Tool_Name", values[0])
	} else if value.Valid {
		t.ToolName = value.String
	}
	return nil
}

// QueryToolOperativerecord queries the Tool_Operativerecord edge of the Tool.
func (t *Tool) QueryToolOperativerecord() *OperativerecordQuery {
	return (&ToolClient{config: t.config}).QueryToolOperativerecord(t)
}

// Update returns a builder for updating this Tool.
// Note that, you need to call Tool.Unwrap() before calling this method, if this Tool
// was returned from a transaction, and the transaction was committed or rolled back.
func (t *Tool) Update() *ToolUpdateOne {
	return (&ToolClient{config: t.config}).UpdateOne(t)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (t *Tool) Unwrap() *Tool {
	tx, ok := t.config.driver.(*txDriver)
	if !ok {
		panic("ent: Tool is not a transactional entity")
	}
	t.config.driver = tx.drv
	return t
}

// String implements the fmt.Stringer.
func (t *Tool) String() string {
	var builder strings.Builder
	builder.WriteString("Tool(")
	builder.WriteString(fmt.Sprintf("id=%v", t.ID))
	builder.WriteString(", Tool_Name=")
	builder.WriteString(t.ToolName)
	builder.WriteByte(')')
	return builder.String()
}

// Tools is a parsable slice of Tool.
type Tools []*Tool

func (t Tools) config(cfg config) {
	for _i := range t {
		t[_i].config = cfg
	}
}
