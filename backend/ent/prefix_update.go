// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"

	"github.com/facebookincubator/ent/dialect/sql"
	"github.com/facebookincubator/ent/dialect/sql/sqlgraph"
	"github.com/facebookincubator/ent/schema/field"
	"github.com/sut63/team08/ent/patient"
	"github.com/sut63/team08/ent/predicate"
	"github.com/sut63/team08/ent/prefix"
)

// PrefixUpdate is the builder for updating Prefix entities.
type PrefixUpdate struct {
	config
	hooks      []Hook
	mutation   *PrefixMutation
	predicates []predicate.Prefix
}

// Where adds a new predicate for the builder.
func (pu *PrefixUpdate) Where(ps ...predicate.Prefix) *PrefixUpdate {
	pu.predicates = append(pu.predicates, ps...)
	return pu
}

// SetPname sets the Pname field.
func (pu *PrefixUpdate) SetPname(s string) *PrefixUpdate {
	pu.mutation.SetPname(s)
	return pu
}

// AddFromprefixIDs adds the fromprefix edge to Patient by ids.
func (pu *PrefixUpdate) AddFromprefixIDs(ids ...int) *PrefixUpdate {
	pu.mutation.AddFromprefixIDs(ids...)
	return pu
}

// AddFromprefix adds the fromprefix edges to Patient.
func (pu *PrefixUpdate) AddFromprefix(p ...*Patient) *PrefixUpdate {
	ids := make([]int, len(p))
	for i := range p {
		ids[i] = p[i].ID
	}
	return pu.AddFromprefixIDs(ids...)
}

// Mutation returns the PrefixMutation object of the builder.
func (pu *PrefixUpdate) Mutation() *PrefixMutation {
	return pu.mutation
}

// RemoveFromprefixIDs removes the fromprefix edge to Patient by ids.
func (pu *PrefixUpdate) RemoveFromprefixIDs(ids ...int) *PrefixUpdate {
	pu.mutation.RemoveFromprefixIDs(ids...)
	return pu
}

// RemoveFromprefix removes fromprefix edges to Patient.
func (pu *PrefixUpdate) RemoveFromprefix(p ...*Patient) *PrefixUpdate {
	ids := make([]int, len(p))
	for i := range p {
		ids[i] = p[i].ID
	}
	return pu.RemoveFromprefixIDs(ids...)
}

// Save executes the query and returns the number of rows/vertices matched by this operation.
func (pu *PrefixUpdate) Save(ctx context.Context) (int, error) {
	if v, ok := pu.mutation.Pname(); ok {
		if err := prefix.PnameValidator(v); err != nil {
			return 0, &ValidationError{Name: "Pname", err: fmt.Errorf("ent: validator failed for field \"Pname\": %w", err)}
		}
	}

	var (
		err      error
		affected int
	)
	if len(pu.hooks) == 0 {
		affected, err = pu.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*PrefixMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			pu.mutation = mutation
			affected, err = pu.sqlSave(ctx)
			mutation.done = true
			return affected, err
		})
		for i := len(pu.hooks) - 1; i >= 0; i-- {
			mut = pu.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, pu.mutation); err != nil {
			return 0, err
		}
	}
	return affected, err
}

// SaveX is like Save, but panics if an error occurs.
func (pu *PrefixUpdate) SaveX(ctx context.Context) int {
	affected, err := pu.Save(ctx)
	if err != nil {
		panic(err)
	}
	return affected
}

// Exec executes the query.
func (pu *PrefixUpdate) Exec(ctx context.Context) error {
	_, err := pu.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pu *PrefixUpdate) ExecX(ctx context.Context) {
	if err := pu.Exec(ctx); err != nil {
		panic(err)
	}
}

func (pu *PrefixUpdate) sqlSave(ctx context.Context) (n int, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   prefix.Table,
			Columns: prefix.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: prefix.FieldID,
			},
		},
	}
	if ps := pu.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if value, ok := pu.mutation.Pname(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: prefix.FieldPname,
		})
	}
	if nodes := pu.mutation.RemovedFromprefixIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   prefix.FromprefixTable,
			Columns: []string{prefix.FromprefixColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: patient.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Clear = append(_spec.Edges.Clear, edge)
	}
	if nodes := pu.mutation.FromprefixIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   prefix.FromprefixTable,
			Columns: []string{prefix.FromprefixColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: patient.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Add = append(_spec.Edges.Add, edge)
	}
	if n, err = sqlgraph.UpdateNodes(ctx, pu.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{prefix.Label}
		} else if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return 0, err
	}
	return n, nil
}

// PrefixUpdateOne is the builder for updating a single Prefix entity.
type PrefixUpdateOne struct {
	config
	hooks    []Hook
	mutation *PrefixMutation
}

// SetPname sets the Pname field.
func (puo *PrefixUpdateOne) SetPname(s string) *PrefixUpdateOne {
	puo.mutation.SetPname(s)
	return puo
}

// AddFromprefixIDs adds the fromprefix edge to Patient by ids.
func (puo *PrefixUpdateOne) AddFromprefixIDs(ids ...int) *PrefixUpdateOne {
	puo.mutation.AddFromprefixIDs(ids...)
	return puo
}

// AddFromprefix adds the fromprefix edges to Patient.
func (puo *PrefixUpdateOne) AddFromprefix(p ...*Patient) *PrefixUpdateOne {
	ids := make([]int, len(p))
	for i := range p {
		ids[i] = p[i].ID
	}
	return puo.AddFromprefixIDs(ids...)
}

// Mutation returns the PrefixMutation object of the builder.
func (puo *PrefixUpdateOne) Mutation() *PrefixMutation {
	return puo.mutation
}

// RemoveFromprefixIDs removes the fromprefix edge to Patient by ids.
func (puo *PrefixUpdateOne) RemoveFromprefixIDs(ids ...int) *PrefixUpdateOne {
	puo.mutation.RemoveFromprefixIDs(ids...)
	return puo
}

// RemoveFromprefix removes fromprefix edges to Patient.
func (puo *PrefixUpdateOne) RemoveFromprefix(p ...*Patient) *PrefixUpdateOne {
	ids := make([]int, len(p))
	for i := range p {
		ids[i] = p[i].ID
	}
	return puo.RemoveFromprefixIDs(ids...)
}

// Save executes the query and returns the updated entity.
func (puo *PrefixUpdateOne) Save(ctx context.Context) (*Prefix, error) {
	if v, ok := puo.mutation.Pname(); ok {
		if err := prefix.PnameValidator(v); err != nil {
			return nil, &ValidationError{Name: "Pname", err: fmt.Errorf("ent: validator failed for field \"Pname\": %w", err)}
		}
	}

	var (
		err  error
		node *Prefix
	)
	if len(puo.hooks) == 0 {
		node, err = puo.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*PrefixMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			puo.mutation = mutation
			node, err = puo.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(puo.hooks) - 1; i >= 0; i-- {
			mut = puo.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, puo.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX is like Save, but panics if an error occurs.
func (puo *PrefixUpdateOne) SaveX(ctx context.Context) *Prefix {
	pr, err := puo.Save(ctx)
	if err != nil {
		panic(err)
	}
	return pr
}

// Exec executes the query on the entity.
func (puo *PrefixUpdateOne) Exec(ctx context.Context) error {
	_, err := puo.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (puo *PrefixUpdateOne) ExecX(ctx context.Context) {
	if err := puo.Exec(ctx); err != nil {
		panic(err)
	}
}

func (puo *PrefixUpdateOne) sqlSave(ctx context.Context) (pr *Prefix, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   prefix.Table,
			Columns: prefix.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: prefix.FieldID,
			},
		},
	}
	id, ok := puo.mutation.ID()
	if !ok {
		return nil, &ValidationError{Name: "ID", err: fmt.Errorf("missing Prefix.ID for update")}
	}
	_spec.Node.ID.Value = id
	if value, ok := puo.mutation.Pname(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: prefix.FieldPname,
		})
	}
	if nodes := puo.mutation.RemovedFromprefixIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   prefix.FromprefixTable,
			Columns: []string{prefix.FromprefixColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: patient.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Clear = append(_spec.Edges.Clear, edge)
	}
	if nodes := puo.mutation.FromprefixIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   prefix.FromprefixTable,
			Columns: []string{prefix.FromprefixColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: patient.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Add = append(_spec.Edges.Add, edge)
	}
	pr = &Prefix{config: puo.config}
	_spec.Assign = pr.assignValues
	_spec.ScanValues = pr.scanValues()
	if err = sqlgraph.UpdateNode(ctx, puo.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{prefix.Label}
		} else if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	return pr, nil
}
