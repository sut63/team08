// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"github.com/facebookincubator/ent/dialect/sql"
	"github.com/sut63/team08/ent/prefix"
)

// Prefix is the model entity for the Prefix schema.
type Prefix struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Pname holds the value of the "Pname" field.
	Pname string `json:"Pname,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PrefixQuery when eager-loading is set.
	Edges PrefixEdges `json:"edges"`
}

// PrefixEdges holds the relations/edges for other nodes in the graph.
type PrefixEdges struct {
	// Fromprefix holds the value of the fromprefix edge.
	Fromprefix []*Patient
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// FromprefixOrErr returns the Fromprefix value or an error if the edge
// was not loaded in eager-loading.
func (e PrefixEdges) FromprefixOrErr() ([]*Patient, error) {
	if e.loadedTypes[0] {
		return e.Fromprefix, nil
	}
	return nil, &NotLoadedError{edge: "fromprefix"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Prefix) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{},  // id
		&sql.NullString{}, // Pname
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Prefix fields.
func (pr *Prefix) assignValues(values ...interface{}) error {
	if m, n := len(values), len(prefix.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	pr.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field Pname", values[0])
	} else if value.Valid {
		pr.Pname = value.String
	}
	return nil
}

// QueryFromprefix queries the fromprefix edge of the Prefix.
func (pr *Prefix) QueryFromprefix() *PatientQuery {
	return (&PrefixClient{config: pr.config}).QueryFromprefix(pr)
}

// Update returns a builder for updating this Prefix.
// Note that, you need to call Prefix.Unwrap() before calling this method, if this Prefix
// was returned from a transaction, and the transaction was committed or rolled back.
func (pr *Prefix) Update() *PrefixUpdateOne {
	return (&PrefixClient{config: pr.config}).UpdateOne(pr)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (pr *Prefix) Unwrap() *Prefix {
	tx, ok := pr.config.driver.(*txDriver)
	if !ok {
		panic("ent: Prefix is not a transactional entity")
	}
	pr.config.driver = tx.drv
	return pr
}

// String implements the fmt.Stringer.
func (pr *Prefix) String() string {
	var builder strings.Builder
	builder.WriteString("Prefix(")
	builder.WriteString(fmt.Sprintf("id=%v", pr.ID))
	builder.WriteString(", Pname=")
	builder.WriteString(pr.Pname)
	builder.WriteByte(')')
	return builder.String()
}

// Prefixes is a parsable slice of Prefix.
type Prefixes []*Prefix

func (pr Prefixes) config(cfg config) {
	for _i := range pr {
		pr[_i].config = cfg
	}
}
